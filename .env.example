# Backend API Configuration
API_BASE_URL=http://localhost:3000/api

# Google Maps (needed for map display in Flutter)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# WithPersona Configuration (for ID verification)
PERSONA_TEMPLATE_ID=your_persona_template_id_here
PERSONA_ENVIRONMENT=sandbox

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual API keys
# 3. Get Google Maps API key from: https://console.cloud.google.com/
#    - Enable Maps SDK for Android/iOS and Geocoding API
# 4. Get WithPersona template ID from: https://app.withpersona.com/
#    - Create an inquiry template for ID verification
#    - Copy the template ID (starts with 'tmpl_')
#    - Use 'sandbox' for testing, 'production' for live verification
# 5. Configure the backend server with all other API keys (OpenWeather, Gemini, etc.)
#    See backend/.env.example for complete API configuration
#
# Note: Most API keys are now managed by the backend server for better security.
# Only Google Maps and WithPersona keys are needed in the Flutter app.